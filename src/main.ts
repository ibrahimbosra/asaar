import './style.css'
import { initializeApp } from 'firebase/app'
import { browserLocalPersistence, createUserWithEmailAndPassword, getAuth, onAuthStateChanged, setPersistence, signInWithEmailAndPassword, signOut, type User } from 'firebase/auth'
import { addDoc, collection, deleteDoc, doc, getDoc, getDocs, getFirestore, onSnapshot, query, setDoc, updateDoc, where, writeBatch } from 'firebase/firestore'

const firebaseConfig = { apiKey: 'AIzaSyAnaTxPXhxMI3A-U4IucbEDfQ8hOTbix_w', authDomain: 'msalma-1d983.firebaseapp.com', projectId: 'msalma-1d983', storageBucket: 'msalma-1d983.firebasestorage.app', messagingSenderId: '1029462661783', appId: '1:1029462661783:web:61a7c2fd8336326fab27d2' }
const app = initializeApp(firebaseConfig)
const auth = getAuth(app)
const db = getFirestore(app)
const appRoot = document.querySelector<HTMLDivElement>('#app')!

type Product = { id: string; name: string; price: number; number: number; storedNumber: boolean; categoryId: string | null }
type Category = { id: string; name: string }
type Settings = { syriaEnabled: boolean; exchangeRate: number; darkMode: boolean }
type BeforeInstallPromptEvent = Event & { prompt: () => Promise<void>; userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }> }
const defaultSettings: Settings = { syriaEnabled: false, exchangeRate: 0, darkMode: false }
let currentUser: User | null = null
let products: Product[] = []
let categories: Category[] = []
const productPageSize = 50
let productDisplayLimit = productPageSize
let selectedCategoryId = 'all'
let settings: Settings = defaultSettings
let unsubscribeProducts: () => void = () => undefined
let unsubscribeCategories: () => void = () => undefined
let deferredInstall: BeforeInstallPromptEvent | null = null
let quickNavTimer: ReturnType<typeof setTimeout> | undefined

const icon = (name: string) => ({ plus: '<path d="M12 5v14M5 12h14"/>', settings: '<path d="M12 3.5a2 2 0 0 1 2 2v.2a6.8 6.8 0 0 1 1.8 1l.2-.1a2 2 0 1 1 2 3.5l-.2.1a6.8 6.8 0 0 1 0 2.1l.2.1a2 2 0 1 1-2 3.5l-.2-.1a6.8 6.8 0 0 1-1.8 1v.2a2 2 0 1 1-4 0v-.2a6.8 6.8 0 0 1-1.8-1l-.2.1a2 2 0 1 1-2-3.5l.2-.1a6.8 6.8 0 0 1 0-2.1l-.2-.1a2 2 0 1 1 2-3.5l.2.1a6.8 6.8 0 0 1 1.8-1v-.2a2 2 0 0 1 2-2Z"/><circle cx="12" cy="12" r="2.5"/>', edit: '<path d="M4 20l4.5-1 9.4-9.4a2.1 2.1 0 0 0-3-3L5.5 16l-1.5 4Z"/><path d="m13.5 6.5 4 4"/>', trash: '<path d="M4 7h16M10 11v6M14 11v6M6 7l1 13h10l1-13M9 7V4h6v3"/>', search: '<circle cx="10.8" cy="10.8" r="6.8"/><path d="m16 16 4 4"/>', arrowUp: '<path d="m6 14 6-6 6 6"/>', arrowDown: '<path d="m6 10 6 6 6-6"/>', close: '<path d="m6 6 12 12M18 6 6 18"/>' }[name] || '')
const svg = (name: string) => `<svg viewBox="0 0 24 24" aria-hidden="true">${icon(name)}</svg>`
const money = (value: number) => Number.isFinite(value) ? new Intl.NumberFormat('en-US', { maximumFractionDigits: 2 }).format(value) : '—'
const syp = (value: number) => Number.isFinite(value) ? new Intl.NumberFormat('en-US', { maximumFractionDigits: 0, useGrouping: true }).format(Math.round(value)) : '—'
const arabicSort = (a: Product, b: Product) => a.name.localeCompare(b.name, 'ar', { sensitivity: 'base' })
const isStandalone = () => window.matchMedia('(display-mode: standalone)').matches || Boolean((navigator as Navigator & { standalone?: boolean }).standalone)
const settingsKey = (uid: string) => `asaar-settings-${uid}`

function escapeHtml(value: string) { return value.replace(/[&<>'"]/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[character] || character)) }
function highlightProductName(value: string, search: string) {
  const term = search.trim()
  if (!term) return escapeHtml(value)
  const pattern = new RegExp(`(${term.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')})`, 'giu')
  return value.split(pattern).map((part) => part.toLocaleLowerCase('ar') === term.toLocaleLowerCase('ar' ) ? `<mark class="search-highlight">${escapeHtml(part)}</mark>` : escapeHtml(part)).join('')
}
function safeSettings(value: unknown): Settings { const data = value as Partial<Settings> | null; return { syriaEnabled: data?.syriaEnabled === true, exchangeRate: typeof data?.exchangeRate === 'number' && Number.isFinite(data.exchangeRate) && data.exchangeRate > 0 ? data.exchangeRate : 0, darkMode: data?.darkMode === true } }
function applyTheme(value: boolean) { document.body.classList.toggle('dark-mode', value) }
function applyStoredTheme() { for (const key of Object.keys(localStorage)) { if (!key.startsWith('asaar-settings-')) continue; try { const cached = JSON.parse(localStorage.getItem(key) || 'null') as Partial<Settings> | null; if (cached?.darkMode === true) { applyTheme(true); return } } catch { /* Ignore malformed cached settings. */ } } }
function loadCachedSettings(uid: string): Settings { try { return safeSettings(JSON.parse(localStorage.getItem(settingsKey(uid)) || 'null')) } catch { return defaultSettings } }
function saveCachedSettings(uid: string, value: Settings) { localStorage.setItem(settingsKey(uid), JSON.stringify(value)) }
function stableNumber(id: string) { let hash = 0; for (const character of id) hash = (hash * 31 + character.charCodeAt(0)) >>> 0; return (hash % 900000) + 100000 }
function parseProduct(id: string, data: Record<string, unknown>): Product | null { const name = typeof data.name === 'string' ? data.name.trim() : ''; const price = typeof data.price === 'number' && Number.isFinite(data.price) && data.price > 0 ? data.price : Number.NaN; if (!name) return null; const storedNumber = typeof data.number === 'number' && Number.isInteger(data.number) && data.number > 0; const categoryId = typeof data.categoryId === 'string' && data.categoryId.trim() ? data.categoryId : null; return { id, name, price, number: storedNumber ? data.number as number : stableNumber(id), storedNumber, categoryId } }
function parseCategory(id: string, data: Record<string, unknown>): Category | null { const name = typeof data.name === 'string' ? data.name.trim() : ''; return name ? { id, name } : null }
function friendlyError(error: unknown) { const code = typeof error === 'object' && error && 'code' in error ? String((error as { code: string }).code) : ''; return code.includes('network') || code.includes('unavailable') ? 'لا يوجد اتصال حاليًا. تحقق من الإنترنت وحاول مجددًا.' : 'تعذر حفظ التغيير. حاول مجددًا.' }
function firestoreErrorMessage(error: unknown) { const code = typeof error === 'object' && error && 'code' in error ? String((error as { code: string }).code) : ''; if (code.includes('permission-denied')) return 'لا تملك صلاحية قراءة هذه المنتجات.'; if (code.includes('unauthenticated')) return 'انتهت جلسة تسجيل الدخول. سجّل الدخول مجددًا.'; if (code.includes('unavailable') || code.includes('network') || code.includes('deadline-exceeded')) return 'تعذر الاتصال حاليًا. تحقق من الإنترنت وحاول مجددًا.'; return 'تعذر تحميل المنتجات حاليًا. حاول مجددًا.' }
function authErrorMessage(error: unknown) { const code = typeof error === 'object' && error && 'code' in error ? String((error as { code: string }).code) : ''; console.error('Firebase Authentication error:', code || error); switch (code) { case 'auth/operation-not-allowed': return 'إنشاء الحساب بالبريد وكلمة المرور غير مفعّل في Firebase. فعّل مزود Email/Password من Authentication ثم Sign-in method.'; case 'auth/email-already-in-use': return 'هذا البريد مستخدم من قبل. جرّب تسجيل الدخول بدل إنشاء حساب جديد.'; case 'auth/invalid-email': return 'أدخل بريدًا إلكترونيًا صحيحًا.'; case 'auth/weak-password': return 'كلمة المرور ضعيفة. استخدم 6 أحرف أو أكثر.'; case 'auth/network-request-failed': return 'تعذر الاتصال بـ Firebase. تحقق من الإنترنت وحاول مجددًا.'; case 'auth/too-many-requests': return 'تمت محاولات كثيرة. انتظر قليلًا ثم حاول مجددًا.'; default: return 'تعذر إنشاء الحساب. تحقق من البريد وكلمة المرور وإعدادات Firebase.' } }

function renderBootstrap(message = 'جارٍ استعادة جلستك...') { appRoot.innerHTML = `<main class="bootstrap"><div class="brand-mark">أس</div><p>${message}</p></main>` }
function renderAuth(error = '') {
  appRoot.innerHTML = `<main class="auth-page"><div class="brand-mark">أس</div><p class="eyebrow">قائمة أسعارك، ببساطة</p><h1>الأسعار</h1><p class="auth-copy">كل أسعار منتجاتك في مكان واحد، جاهزة عندما تحتاجها.</p><form id="auth-form" class="auth-form"><label>البريد الإلكتروني<input name="email" type="email" autocomplete="email" placeholder="name@example.com" required></label><label>كلمة المرور<input name="password" type="password" autocomplete="current-password" minlength="6" placeholder="••••••••" required></label><p class="form-error">${escapeHtml(error)}</p><button class="primary-button" type="submit" id="auth-submit">تسجيل الدخول</button><button class="text-button" type="button" id="toggle-auth">إنشاء حساب جديد</button></form></main>`
  let signUp = false
  document.querySelector('#toggle-auth')!.addEventListener('click', () => { signUp = !signUp; document.querySelector('#auth-submit')!.textContent = signUp ? 'إنشاء الحساب' : 'تسجيل الدخول'; document.querySelector('#toggle-auth')!.textContent = signUp ? 'لديك حساب؟ تسجيل الدخول' : 'إنشاء حساب جديد' })
  document.querySelector<HTMLFormElement>('#auth-form')!.addEventListener('submit', async (event) => { event.preventDefault(); const form = new FormData(event.currentTarget as HTMLFormElement); const button = document.querySelector<HTMLButtonElement>('#auth-submit')!; button.disabled = true; button.textContent = 'جارٍ المتابعة...'; try { const email = String(form.get('email')); const password = String(form.get('password')); if (signUp) await createUserWithEmailAndPassword(auth, email, password); else await signInWithEmailAndPassword(auth, email, password) } catch (submitError) { renderAuth(authErrorMessage(submitError)) } })
}

function renderProductsShell() {
  applyTheme(settings.darkMode)
  appRoot.innerHTML = `<main class="products-page"><header class="topbar"><div class="search-box">${svg('search')}<input id="search" type="search" placeholder="ابحث عن منتج..." autocomplete="off"></div><button class="icon-button add-button" id="add-product" aria-label="إضافة منتج">${svg('plus')}</button><button class="icon-button" id="open-settings" aria-label="الإعدادات">${svg('settings')}</button></header><nav class="category-bar" id="category-bar" aria-label="الفئات"></nav><section class="list-head"><button class="install-button hidden" id="install-app">تثبيت التطبيق</button></section><section class="products-list" id="products-list"></section><button class="load-more-products hidden" id="load-more-products" type="button"></button><div class="quick-nav" id="quick-nav"><button id="scroll-top" aria-label="إلى الأعلى">${svg('arrowUp')}</button><button id="scroll-bottom" aria-label="إلى الأسفل">${svg('arrowDown')}</button></div></main>`
  document.querySelector<HTMLInputElement>('#search')!.addEventListener('input', () => { productDisplayLimit = productPageSize; updateProductList() })
  document.querySelector('#add-product')!.addEventListener('click', renderAddModal)
  document.querySelector('#open-settings')!.addEventListener('click', renderSettingsNew)
  document.querySelector('#scroll-top')!.addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }))
  document.querySelector('#scroll-bottom')!.addEventListener('click', () => window.scrollTo({ top: document.body.scrollHeight, behavior: 'smooth' }))
  document.querySelector('#load-more-products')!.addEventListener('click', () => { productDisplayLimit += productPageSize; updateProductList() })
  window.onscroll = handleScroll
  updateInstallButton()
  updateCategoryBar()
  updateProductList()
  updateNavigation()
}
function updateProductList() {
  const list = document.querySelector<HTMLElement>('#products-list')
  const count = document.querySelector('#product-count')
  const search = document.querySelector<HTMLInputElement>('#search')?.value.trim().toLocaleLowerCase('ar') || ''
  if (!list) return
  const visible = products.filter((product) => (selectedCategoryId === 'all' || product.categoryId === selectedCategoryId) && product.name.toLocaleLowerCase('ar').includes(search)).sort(arabicSort)
  const displayed = visible.slice(0, productDisplayLimit)
  if (count) count.textContent = String(products.length)
  list.innerHTML = visible.length ? displayed.map((product, index) => `
    <article class="product-card" data-edit="${product.id}" tabindex="0" role="button" aria-label="تعديل ${escapeHtml(product.name)}">
      <div class="product-top">
        <span class="product-category">${escapeHtml(categories.find((category) => category.id === product.categoryId)?.name || 'بدون فئة')}</span>
        <h2 class="product-name">${highlightProductName(product.name, search)}</h2>
      </div>
      <span class="product-number">${index + 1}</span>
      <div class="price-row">
        <div class="price-box usd-price">
          <span class="price-label">السعر بالدولار</span>
          <div class="price-value"><span class="currency-symbol">$</span><span class="price-number">${money(product.price)}</span></div>
        </div>
        <div class="price-box syp-price ${settings.syriaEnabled ? '' : 'is-disabled'}">
          <span class="price-label">السعر بالليرة</span>
          <div class="price-value">${settings.syriaEnabled ? '<span class="currency-word">ليرة</span><span class="price-number">' + syp(product.price * settings.exchangeRate) + '</span>' : '<span class="syria-box-hint">«فعّل الليرة من الإعدادات»</span>'}</div>
        </div>
      </div>
    </article>
  `).join('') : `<div class="empty-state"><div class="empty-icon">⌕</div><h2>${products.length ? 'لا توجد نتائج' : 'ابدأ بإضافة أول منتج'}</h2><p>${products.length ? 'جرّب كلمة بحث أو فئة مختلفة.' : 'احتفظ بأسعارك اليومية في قائمة واحدة واضحة.'}</p></div>`
  list.querySelectorAll<HTMLElement>('.product-card[data-edit]').forEach((card) => {
    const openEdit = () => renderEditModal(card.dataset.edit!)
    card.addEventListener('click', openEdit)
    card.addEventListener('keydown', (event) => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); openEdit() } })
  })
  const loadMore = document.querySelector<HTMLButtonElement>('#load-more-products')
  if (loadMore) { const remaining = visible.length - displayed.length; loadMore.classList.toggle('hidden', remaining <= 0); loadMore.textContent = remaining > 0 ? `عرض المزيد (${remaining})` : '' }
}
function updateCategoryBar() { const bar = document.querySelector<HTMLElement>('#category-bar'); if (!bar) return; bar.innerHTML = [{ id: 'all', name: 'الكل' }, ...categories].map((category) => { const count = category.id === 'all' ? products.length : categoryProductCount(category.id); return `<button class="category-chip ${selectedCategoryId === category.id ? 'active' : ''}" data-category="${category.id}"><span>${escapeHtml(category.name)}</span><span class="category-count" ${category.id === 'all' ? 'id="product-count"' : ''}>${count}</span></button>` }).join(''); bar.querySelectorAll<HTMLButtonElement>('[data-category]').forEach((button) => button.addEventListener('click', () => { selectedCategoryId = button.dataset.category!; productDisplayLimit = productPageSize; updateCategoryBar(); updateProductList() })) }
function renderProductsError(error: unknown) { const list = document.querySelector<HTMLElement>('#products-list'); if (!list) return; list.innerHTML = `<div class="empty-state"><div class="empty-icon">!</div><h2>تعذر تحميل المنتجات</h2><p>${firestoreErrorMessage(error)}</p></div>` }
function updateInstallButton() { const button = document.querySelector<HTMLButtonElement>('#install-app'); if (!button) return; button.classList.toggle('hidden', !deferredInstall || isStandalone()); if (!button.classList.contains('hidden')) button.onclick = installPwa }
function updateNavigation() { const nav = document.querySelector<HTMLElement>('#quick-nav'); const top = document.querySelector<HTMLButtonElement>('#scroll-top'); const bottom = document.querySelector<HTMLButtonElement>('#scroll-bottom'); if (!nav || !top || !bottom) return; const atTop = window.scrollY < 80; const atBottom = window.innerHeight + window.scrollY >= document.documentElement.scrollHeight - 80; top.classList.toggle('hidden', atTop); bottom.classList.toggle('hidden', atBottom); nav.classList.toggle('has-actions', !atTop || !atBottom) }
function handleScroll() { updateNavigation(); const nav = document.querySelector<HTMLElement>('#quick-nav'); if (!nav) return; nav.classList.add('visible'); if (quickNavTimer) clearTimeout(quickNavTimer); quickNavTimer = setTimeout(() => nav.classList.remove('visible'), 3000) }

const modal = (content: string) => { const element = document.createElement('div'); element.className = 'modal-backdrop'; element.innerHTML = `<section class="modal">${content}</section>`; element.addEventListener('click', (event) => { if (event.target === element) element.remove() }); document.body.append(element); return element }
function categoryOptions(selected: string | null = null) { return `<option value="">بدون فئة</option>${categories.map((category) => `<option value="${category.id}" ${selected === category.id ? 'selected' : ''}>${escapeHtml(category.name)}</option>`).join('')}` }
function renderAddModal() { const element = modal(`<button class="modal-close" aria-label="إغلاق">${svg('close')}</button><p class="eyebrow">منتج جديد</p><h2>إضافة منتج</h2><form id="product-form"><label>اسم المنتج<input name="name" required autofocus maxlength="100" placeholder="مثال: دولاب داخلي صيني"></label><label>السعر بالدولار<input name="price" type="number" min="0.01" step="0.01" placeholder="0.00"></label><label>الفئة<select name="categoryId">${categoryOptions()}</select></label><p class="modal-error" id="modal-error"></p><button class="primary-button" type="submit">حفظ المنتج</button></form>`); element.querySelector('.modal-close')!.addEventListener('click', () => element.remove()); element.querySelector('form')!.addEventListener('submit', async (event) => { event.preventDefault(); const form = new FormData(event.currentTarget as HTMLFormElement); const name = String(form.get('name')).trim(); const priceInput = String(form.get('price') || '').trim(); const price = priceInput ? Number(priceInput) : Number.NaN; const categoryId = String(form.get('categoryId') || '') || null; const error = element.querySelector('#modal-error')!; if (!name || (priceInput && (!Number.isFinite(price) || price <= 0)) || !currentUser) { error.textContent = priceInput ? 'أدخل سعرًا صالحًا.' : 'أدخل اسمًا صالحًا.'; return } const button = element.querySelector<HTMLButtonElement>('button[type="submit"]')!; const productNumber = nextProductNumber(); button.disabled = true; try { await addDoc(collection(db, 'users', currentUser.uid, 'products'), { name, price: Number.isFinite(price) ? price : null, number: productNumber, categoryId }); element.remove() } catch (addError) { button.disabled = false; error.textContent = friendlyError(addError) } }) }
function renderEditModal(id: string) { const product = products.find((item) => item.id === id); if (!product || !currentUser) return; const element = modal(`<button class="modal-close" aria-label="إغلاق">${svg('close')}</button><p class="eyebrow">تعديل المنتج</p><h2>${escapeHtml(product.name)}</h2><form id="edit-form"><label>اسم المنتج<input name="name" required maxlength="100" value="${escapeHtml(product.name)}"></label><label>السعر بالدولار<input name="price" type="number" min="0.01" step="0.01" value="${Number.isFinite(product.price) ? product.price : ''}"></label><label>الفئة<select name="categoryId">${categoryOptions(product.categoryId)}</select></label><p class="modal-error" id="modal-error"></p><div class="modal-actions"><button class="primary-button" type="submit">حفظ التعديل</button><button class="danger-button" id="delete-product" type="button">حذف المنتج</button></div></form>`); element.querySelector('.modal-close')!.addEventListener('click', () => element.remove()); element.querySelector('#delete-product')!.addEventListener('click', () => { element.remove(); renderDeleteModal(id) }); element.querySelector('form')!.addEventListener('submit', async (event) => { event.preventDefault(); const form = new FormData(event.currentTarget as HTMLFormElement); const name = String(form.get('name')).trim(); const priceInput = String(form.get('price') || '').trim(); const price = priceInput ? Number(priceInput) : Number.NaN; const categoryId = String(form.get('categoryId') || '') || null; const error = element.querySelector('#modal-error')!; if (!name || (priceInput && (!Number.isFinite(price) || price <= 0))) { error.textContent = priceInput ? 'أدخل سعرًا صالحًا.' : 'أدخل اسمًا صالحًا.'; return } const button = element.querySelector<HTMLButtonElement>('button[type="submit"]')!; button.disabled = true; try { await updateDoc(doc(db, 'users', currentUser!.uid, 'products', id), { name, price: Number.isFinite(price) ? price : null, categoryId }); element.remove() } catch (updateError) { button.disabled = false; error.textContent = friendlyError(updateError) } }) }
function nextProductNumber() { const numbers = products.filter((product) => product.storedNumber).map((product) => product.number); return (numbers.length ? Math.max(...numbers) : 0) + 1 }
function renderDeleteModal(id: string) { const product = products.find((item) => item.id === id); if (!product || !currentUser) return; const element = modal(`<p class="eyebrow">تأكيد الحذف</p><h2>حذف ${escapeHtml(product.name)}؟</h2><p class="modal-copy">سيتم إزالة هذا المنتج من قائمتك.</p><p class="modal-error" id="modal-error"></p><div class="modal-actions"><button class="secondary-button" id="cancel-delete">إلغاء</button><button class="danger-button" id="confirm-delete">حذف المنتج</button></div>`); element.querySelector('#cancel-delete')!.addEventListener('click', () => element.remove()); element.querySelector('#confirm-delete')!.addEventListener('click', async () => { const button = element.querySelector<HTMLButtonElement>('#confirm-delete')!; button.disabled = true; try { await deleteDoc(doc(db, 'users', currentUser!.uid, 'products', id)); products = products.filter((item) => item.id !== id); updateProductList(); element.remove() } catch (deleteError) { button.disabled = false; element.querySelector('#modal-error')!.textContent = friendlyError(deleteError) } }) }
function renderSettingsNew() { if (!currentUser) return; const element = modal(`<button class="modal-close" aria-label="إغلاق">${svg('close')}</button><p class="eyebrow">تفضيلاتك</p><div class="settings-tabs"><button class="active" data-settings-tab="currency">العملة</button><button data-settings-tab="categories">الفئات</button><button data-settings-tab="system">النظام</button></div><div id="settings-content"></div>`); element.querySelector('.modal-close')!.addEventListener('click', () => element.remove()); const content = element.querySelector<HTMLElement>('#settings-content')!; const renderTab = (tab: string) => { element.querySelectorAll<HTMLButtonElement>('[data-settings-tab]').forEach((button) => button.classList.toggle('active', button.dataset.settingsTab === tab)); if (tab === 'currency') { content.innerHTML = `<h2>العملة</h2><label class="switch-row"><span>تفعيل الليرة السورية</span><input id="syria-toggle" type="checkbox" ${settings.syriaEnabled ? 'checked' : ''}><i></i></label><div id="rate-field" class="rate-field ${settings.syriaEnabled ? '' : 'hidden'}"><label>سعر الصرف<input id="exchange-rate" type="number" min="1" step="1" value="${settings.exchangeRate || ''}" placeholder="15000"><small>1 USD = ليرة سورية</small></label></div><p class="modal-error" id="modal-error"></p><button class="primary-button" id="save-settings">حفظ الإعدادات</button>`; const toggle = content.querySelector<HTMLInputElement>('#syria-toggle')!; toggle.addEventListener('change', () => content.querySelector('#rate-field')!.classList.toggle('hidden', !toggle.checked)); content.querySelector('#save-settings')!.addEventListener('click', async () => { const exchangeRate = Number(content.querySelector<HTMLInputElement>('#exchange-rate')!.value); const error = content.querySelector('#modal-error')!; if (toggle.checked && (!Number.isFinite(exchangeRate) || exchangeRate <= 0)) { error.textContent = 'أدخل سعر صرف صالحًا.'; return } const next = { ...settings, syriaEnabled: toggle.checked, exchangeRate: toggle.checked ? exchangeRate : settings.exchangeRate }; const button = content.querySelector<HTMLButtonElement>('#save-settings')!; button.disabled = true; try { await setDoc(doc(db, 'users', currentUser!.uid), next, { merge: true }); settings = next; saveCachedSettings(currentUser!.uid, settings); updateProductList(); element.remove() } catch (saveError) { button.disabled = false; error.textContent = friendlyError(saveError) } }) } else if (tab === 'categories') renderCategoriesTab(content); else { content.innerHTML = `<h2>النظام</h2><div class="account"><span>${escapeHtml(currentUser!.email || '')}</span><button class="text-button" id="logout">تسجيل الخروج</button></div>`; content.querySelector('#logout')!.addEventListener('click', () => { document.querySelectorAll<HTMLElement>('.modal-backdrop').forEach((modalElement) => modalElement.remove()); void signOut(auth) }) } }; element.querySelectorAll<HTMLButtonElement>('[data-settings-tab]').forEach((button) => button.addEventListener('click', () => renderTab(button.dataset.settingsTab!))); element.querySelector<HTMLButtonElement>('[data-settings-tab="system"]')!.addEventListener('click', () => { const heading = content.querySelector('h2'); if (!heading || content.querySelector('#dark-mode-toggle')) return; heading.insertAdjacentHTML('afterend', `<label class="switch-row"><span>الوضع الليلي</span><input id="dark-mode-toggle" type="checkbox" ${settings.darkMode ? 'checked' : ''}><i></i></label>`); const toggle = content.querySelector<HTMLInputElement>('#dark-mode-toggle')!; toggle.addEventListener('change', async () => { const next = { ...settings, darkMode: toggle.checked }; settings = next; applyTheme(next.darkMode); saveCachedSettings(currentUser!.uid, next); try { await setDoc(doc(db, 'users', currentUser!.uid), { darkMode: next.darkMode }, { merge: true }) } catch { /* Keep the local theme when the network is unavailable. */ } }) }); renderTab('currency') }
function renderCategoriesTab(content: HTMLElement) { content.innerHTML = `<h2>الفئات</h2><form id="category-form" class="inline-form"><input name="name" maxlength="50" required placeholder="اسم الفئة"><button class="primary-button" type="submit">إنشاء فئة</button></form><p class="modal-error" id="category-error"></p><div class="category-list">${categories.length ? categories.map((category) => `<div class="category-row" data-manage="${category.id}"><button class="category-name" data-manage="${category.id}">${escapeHtml(category.name)} <span>${categoryProductCount(category.id)} ${categoryProductCount(category.id) === 1 ? 'منتج' : 'منتج'}</span></button><button class="secondary-button" data-bulk="${category.id}">إضافة منتجات</button></div>`).join('') : '<p class="empty-copy">لا توجد فئات بعد.</p>'}</div>`; content.querySelector<HTMLFormElement>('#category-form')!.addEventListener('submit', async (event) => { event.preventDefault(); if (!currentUser) return; const name = String(new FormData(event.currentTarget as HTMLFormElement).get('name')).trim(); const error = content.querySelector('#category-error')!; if (!name) { error.textContent = 'أدخل اسم الفئة.'; return } const normalizedName = name.toLocaleLowerCase('ar'); if (categories.some((category) => category.name.toLocaleLowerCase('ar') === normalizedName)) { error.textContent = 'هذه الفئة موجودة بالفعل.'; return } const button = content.querySelector<HTMLButtonElement>('button[type="submit"]')!; button.disabled = true; try { const duplicate = await getDocs(query(collection(db, 'users', currentUser.uid, 'categories'), where('name', '==', name))); if (!duplicate.empty) { error.textContent = 'هذه الفئة موجودة بالفعل.'; button.disabled = false; return } await addDoc(collection(db, 'users', currentUser.uid, 'categories'), { name }); renderCategoriesTab(content) } catch (categoryError) { button.disabled = false; error.textContent = friendlyError(categoryError) } }); content.querySelectorAll<HTMLButtonElement>('[data-manage]').forEach((button) => button.addEventListener('click', (event) => { event.stopPropagation(); renderCategoryManagement(button.dataset.manage!) })); content.querySelectorAll<HTMLButtonElement>('[data-bulk]').forEach((button) => button.addEventListener('click', (event) => { event.stopPropagation(); renderBulkModal(button.dataset.bulk!) })) }
function categoryProductCount(categoryId: string) { return products.filter((product) => product.categoryId === categoryId).length }
function renderCategoryManagement(id: string) { const category = categories.find((item) => item.id === id); if (!category || !currentUser) return; const element = modal(`<button class="modal-close" aria-label="إغلاق">${svg('close')}</button><p class="eyebrow">إدارة الفئة</p><h2>${escapeHtml(category.name)}</h2><form id="rename-category"><label>اسم الفئة<input name="name" maxlength="50" required value="${escapeHtml(category.name)}"></label><p class="modal-error" id="modal-error"></p><button class="primary-button" type="submit">حفظ الاسم</button></form><button class="danger-button category-delete" id="delete-category">حذف الفئة</button>`); element.querySelector('.modal-close')!.addEventListener('click', () => element.remove()); element.querySelector<HTMLFormElement>('#rename-category')!.addEventListener('submit', async (event) => { event.preventDefault(); const name = String(new FormData(event.currentTarget as HTMLFormElement).get('name')).trim(); const error = element.querySelector('#modal-error')!; if (!name) { error.textContent = 'أدخل اسم الفئة.'; return } const normalizedName = name.toLocaleLowerCase('ar'); if (categories.some((item) => item.id !== id && item.name.toLocaleLowerCase('ar') === normalizedName)) { error.textContent = 'هذه الفئة موجودة بالفعل.'; return } const button = element.querySelector<HTMLButtonElement>('button[type="submit"]')!; button.disabled = true; try { const duplicate = await getDocs(query(collection(db, 'users', currentUser!.uid, 'categories'), where('name', '==', name))); if (duplicate.docs.some((item) => item.id !== id)) { error.textContent = 'هذه الفئة موجودة بالفعل.'; button.disabled = false; return } await updateDoc(doc(db, 'users', currentUser!.uid, 'categories', id), { name }); const localCategory = categories.find((item) => item.id === id); if (localCategory) localCategory.name = name; categories.sort((a, b) => a.name.localeCompare(b.name, 'ar')); document.querySelectorAll<HTMLButtonElement>(`.category-name[data-manage="${id}"]`).forEach((nameButton) => { nameButton.innerHTML = `${escapeHtml(name)} <span>${categoryProductCount(id)} منتج</span>` }); updateCategoryBar(); updateProductList(); element.remove() } catch (renameError) { button.disabled = false; error.textContent = friendlyError(renameError) } }); element.querySelector('#delete-category')!.addEventListener('click', () => renderCategoryDeleteConfirmation(id, element)) }
function renderCategoryDeleteConfirmation(id: string, parent: HTMLElement) { const category = categories.find((item) => item.id === id); if (!category || !currentUser) return; const element = modal(`<p class="eyebrow">تأكيد الحذف</p><h2>حذف فئة ${escapeHtml(category.name)}؟</h2><p class="modal-copy">ستصبح المنتجات التابعة لها بدون فئة، ولن يتم حذف المنتجات.</p><p class="modal-error" id="modal-error"></p><div class="modal-actions"><button class="secondary-button" id="cancel-category-delete">إلغاء</button><button class="danger-button" id="confirm-category-delete">حذف الفئة</button></div>`); element.querySelector('#cancel-category-delete')!.addEventListener('click', () => element.remove()); element.querySelector('#confirm-category-delete')!.addEventListener('click', async () => { const button = element.querySelector<HTMLButtonElement>('#confirm-category-delete')!; button.disabled = true; try { const affected = products.filter((product) => product.categoryId === id); if (!affected.length) await deleteDoc(doc(db, 'users', currentUser!.uid, 'categories', id)); for (let index = 0; index < affected.length; index += 499) { const batch = writeBatch(db); affected.slice(index, index + 499).forEach((product) => batch.update(doc(db, 'users', currentUser!.uid, 'products', product.id), { categoryId: null })); if (index + 499 >= affected.length) batch.delete(doc(db, 'users', currentUser!.uid, 'categories', id)); await batch.commit() } categories = categories.filter((item) => item.id !== id); if (selectedCategoryId === id) selectedCategoryId = 'all'; document.querySelector<HTMLElement>(`.category-row[data-manage="${id}"]`)?.remove(); element.remove(); parent.remove(); updateCategoryBar(); updateProductList() } catch (deleteError) { button.disabled = false; element.querySelector('#modal-error')!.textContent = friendlyError(deleteError) } }) }
function renderBulkModal(categoryId: string) { if (!currentUser) return; const available = () => products.filter((product) => !product.categoryId); const element = modal(`<button class="modal-close" aria-label="إغلاق">${svg('close')}</button><p class="eyebrow">إضافة منتجات</p><h2>${escapeHtml(categories.find((category) => category.id === categoryId)?.name || '')}</h2><input id="bulk-search" type="search" placeholder="ابحث عن منتج..."><div id="bulk-list" class="bulk-list"></div><p class="modal-error" id="modal-error"></p><button class="primary-button" id="save-bulk">إضافة المحدد</button>`); const list = element.querySelector<HTMLElement>('#bulk-list')!; const renderList = () => { const term = element.querySelector<HTMLInputElement>('#bulk-search')!.value.trim().toLocaleLowerCase('ar'); list.innerHTML = available().filter((product) => product.name.toLocaleLowerCase('ar').includes(term)).map((product) => `<label class="bulk-item"><input type="checkbox" value="${product.id}"><span>${escapeHtml(product.name)}</span></label>`).join('') || '<p class="empty-copy">لا توجد منتجات غير مصنفة.</p>' }; element.querySelector('.modal-close')!.addEventListener('click', () => element.remove()); element.querySelector('#bulk-search')!.addEventListener('input', renderList); element.querySelector('#save-bulk')!.addEventListener('click', async () => { const ids = [...element.querySelectorAll<HTMLInputElement>('#bulk-list input:checked')].map((input) => input.value); if (!ids.length) { element.querySelector('#modal-error')!.textContent = 'اختر منتجًا واحدًا على الأقل.'; return } const batch = writeBatch(db); ids.filter((id) => available().some((product) => product.id === id)).forEach((id) => batch.update(doc(db, 'users', currentUser!.uid, 'products', id), { categoryId })); const button = element.querySelector<HTMLButtonElement>('#save-bulk')!; button.disabled = true; try { await batch.commit(); element.remove() } catch (bulkError) { button.disabled = false; element.querySelector('#modal-error')!.textContent = friendlyError(bulkError) } }); renderList() }

async function loadSettings(uid: string) { settings = loadCachedSettings(uid); try { const snapshot = await getDoc(doc(db, 'users', uid)); settings = snapshot.exists() ? safeSettings(snapshot.data()) : defaultSettings; saveCachedSettings(uid, settings) } catch { /* Keep the per-user cache without blocking the products view. */ } }
async function startAuth() { applyStoredTheme(); renderBootstrap(); try { await setPersistence(auth, browserLocalPersistence) } catch { /* Firebase will still report the current auth state. */ } onAuthStateChanged(auth, async (user) => { currentUser = user; unsubscribeProducts(); unsubscribeCategories(); products = []; categories = []; selectedCategoryId = 'all'; if (!user) { settings = defaultSettings; applyTheme(false); renderAuth(); return } await loadSettings(user.uid); if (currentUser?.uid !== user.uid) return; renderProductsShell(); const productsQuery = query(collection(db, 'users', user.uid, 'products')); unsubscribeProducts = onSnapshot(productsQuery, (snapshot) => { products = snapshot.docs.map((item) => parseProduct(item.id, item.data())).filter((item): item is Product => item !== null).sort(arabicSort); updateProductList() }, (error) => renderProductsError(error)); const categoriesQuery = query(collection(db, 'users', user.uid, 'categories')); unsubscribeCategories = onSnapshot(categoriesQuery, (snapshot) => { categories = snapshot.docs.map((item) => parseCategory(item.id, item.data())).filter((item): item is Category => item !== null).sort((a, b) => a.name.localeCompare(b.name, 'ar')); updateCategoryBar(); updateProductList() }) }) }
async function installPwa() { if (!deferredInstall || isStandalone()) return; deferredInstall.prompt(); await deferredInstall.userChoice; deferredInstall = null; updateInstallButton() }
window.addEventListener('beforeinstallprompt', (event) => { event.preventDefault(); deferredInstall = event as BeforeInstallPromptEvent; updateInstallButton() })
window.addEventListener('appinstalled', () => { deferredInstall = null; updateInstallButton() })
if ('serviceWorker' in navigator) window.addEventListener('load', () => navigator.serviceWorker.register(`${import.meta.env.BASE_URL}sw.js`, { scope: import.meta.env.BASE_URL }))
startAuth()
